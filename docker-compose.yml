services:
  counter:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3001:3000
    volumes:
      - counter_db:/app/db
    environment:
      PORT: 3000
      DATABASE_PATH: /app/db/counter.db
    restart: unless-stopped

volumes:
  counter_db:
